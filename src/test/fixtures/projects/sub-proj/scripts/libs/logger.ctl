void logDebug(string message)
{
  // This is a simple logger function for debugging purposes
  // In a real implementation, this could write to a file or console
  // For this example, we will just print the message to the console
  DebugN("DEBUG: " + message);
}