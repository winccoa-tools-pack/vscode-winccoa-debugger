# test-workspace

Empty VS Code workspace folder used by `@vscode/test-cli` when running
integration and E2E tests via `vscode-test --label <name>`.

The workspace is intentionally empty — tests set up their own state.
